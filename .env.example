# OpenAI
OPENAI_API_KEY=sk-your-key-here

# Database (leave empty for local SQLite, or use PostgreSQL URL for production)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Frontend URL for CORS (production)
# FRONTEND_URL=https://your-app.vercel.app

# Google Calendar (for cloud deployment, base64 encode your credentials.json)
# GOOGLE_CREDENTIALS_JSON=base64-encoded-credentials
# GOOGLE_TOKEN_JSON=base64-encoded-token

# Work hours
WORK_START_HOUR=9
WORK_END_HOUR=18

# Timezone
TIMEZONE=America/New_York
